Generate {batch_size} synthetic training examples.

Schema:
{schema}

Dataset goal:
Create realistic multilingual examples for training a robot assistant to classify and respond to user interruptions while the robot is speaking.

Required standard format:
Every line must be one JSON object with exactly these fields:
- input
- output
- interruption_class
- language
- category
- difficulty

The input string must always use this exact dialogue pattern:
Robot: "<robot is speaking>" User: "<user interrupts>"

The output string must always be a short robot response or action, without "Robot:".

Valid interruption_class values only:
- continue
- stop
- pause
- clarify_or_repeat
- change_or_correct

Valid category values only:
- home_robot
- navigation
- object_handling
- safety
- conversation
- general

Valid difficulty values only:
- easy
- medium
- hard

Language diversity:
- Include English plus other possible robot-user languages when batch size allows.
- Keep each example internally consistent: if language is French, both input and output should be French; if Arabic, both should be Arabic; etc.
- Use the English class labels exactly as written even for non-English examples.

Return exactly {batch_size} JSONL lines.
Return only JSONL.
